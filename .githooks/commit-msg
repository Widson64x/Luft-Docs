#!/bin/sh

if command -v python >/dev/null 2>&1; then
    python scripts/validate_commit_messages.py --message-file "$1"
elif command -v py >/dev/null 2>&1; then
    py -3 scripts/validate_commit_messages.py --message-file "$1"
else
    echo "Python nao encontrado no PATH." >&2
    exit 1
fi